# Example env file for local development.
# DO NOT commit real secret keys. Replace placeholders with your own values.

# Supabase (public keys safe for frontend; keep service role server-only)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=pk.anon.your_public_anon_key

